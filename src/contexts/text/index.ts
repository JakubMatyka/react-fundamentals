// Barrel exports for clean imports
export { TextProvider } from "./provider";
export { useTextContext } from "./context";
export type {
  TextContextType,
  TextProviderProps,
  TextState,
  TextAction,
} from "./types";
